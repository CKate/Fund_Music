<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :latest_single %><br>
    <%= f.text_area :latest_single %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :genre %><br>
    <%= f.select :genre, options_for_select([["Soul/R&B"], ["Pop"], ["Rock"], ["Jazz"], ["Reggae"], ["Hip Hop/Rap"]]) %>
  </div>
  <div class="field">
    <%= f.label :minimum_donation %><br>
    <%= f.text_field :minimum_donation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
