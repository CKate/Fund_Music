<!DOCTYPE html>
<html>
<head>
  <title>FundMusic</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <link href='https://fonts.googleapis.com/css?family=Indie+Flower' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Quicksand' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Music Fund", pages_home_path, :class=> "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%=link_to "About Us", pages_about_path %></li>
        <li><%= link_to "Artists", artists_path%></li>
		<li><%= mail_to "casselyn.samson@gmail.com", "Contact Us"%></li>
      </ul>
	  
	  <%= form_tag(search_path, :method=>"get", :class=>"navbar-form navbar-left", :role=>"search")  do%>
        <div class="form-group">
			<%= text_field_tag(:query, params[:query], class: 'form-control')%>
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
		  <% if !logged_in? %>
			<li><%= link_to "Sign up", new_user_path %></li> 
			<li><%= link_to "Log in", login_path %></li>
		  <% else %>
			<li><%= link_to "Signed in as " + @current_user.user_name, @current_user  %></li>
			<li><%= link_to "Log out", logout_path %></li>
		  <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class = "text-center noticeMessage">
	<% flash.each do |key, value| %>
		<div class="flash <%= key %>">
			<%= value %>
		</div>
	<% end %>
</div>
</br>
<%= yield %>

</br></br>
	<footer class="footer">
		</br>
      <div class="container">
        <p class="text-muted">Kate Samson | <%=link_to "My Github account", 'https://github.com/CKate' %></p>  
      </div>
	</footer>
</body>
</html>
